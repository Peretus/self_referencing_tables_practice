<%= form_for(@weighin) do |f| %>
  <% if @weighin.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@weighin.errors.count, "error") %> prohibited this weighin from being saved:</h2>

      <ul>
      <% @weighin.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
